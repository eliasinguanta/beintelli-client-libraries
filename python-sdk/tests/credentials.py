"""Containing class Credential """
from enum import Enum

class Credential(Enum):
    """A Class containing correct credentials for unittests"""
    USERNAME = "******" #replaced
    PASSWORD = "******" #replaced
